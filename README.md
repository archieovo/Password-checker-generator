# Strong-password-generator/checker
Used Python to create a strong password generator based on NIST guidelines and also added a feature that allows you to test the strength of a password and if that password has been breached

### Installation
Install Python3, download repository and install requirements using `python3 -m pip install -r requirements.txt`

### Usage
To run the password checker/generator use the command `python3 main.py`
